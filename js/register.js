const form=document.querySelector("#registerForm")
const reg_mail=document.getElementById("register_Mail")
form.addEventListener('submit',(e)=>{
    e.preventDefault();
    window.location.href="../index.html"
})
